--- 
🪐STRATUM: m08
GLYPH: 🕸️
🧬DNA: EdgeGraph
🌈SPECTRUM:
  - sigma
  - md
⚖️PHYSICS:
  🪨MASS: 2kb
  🌀ENTROPY: Low
  🧩COMPLEXITY: O(N)
🧬PROVIDES:
  - capability: discover
    signature: "needs -> edges"
    cost: "O(N)"
  - capability: collapse
    signature: "edge -> materialize"
    cost: "O(N)"
  - contract: "prefer lowest cost"
---


# The Edge Graph Protocol

"Deno не встановлюється — він знаходить ребро."

We model **Needs ↔ Possibilities** as addressable edges, not files.

## 1. Addressable Edge

Format:
```
edge://<realm>/<capability>/<version>
```

Examples:
```
edge://brew/rust/1.75
edge://sh/deno/1.39
edge://wasm/go/1.21
edge://dns-txt/I.sigma/v0
```

Each edge is:
- hashable (blake3-16)
- signed (ed25519)
- cacheable (TTL)
- weighted (🪨MASS + 🌀ENTROPY)

## 2. Edge as σ-file

```yaml
---
GLYPH: edge-brew-node-v18
🧬DNA: edge brew node v18
⚡ENERGY: 1
🌈SPECTRUM:
  - sh
  - dns
⚖️PHYSICS:
  🪨MASS: 0.1
  🌀ENTROPY: 0.05
---

@[sh]
# brew list node@18 ...

@[dns]
# TXT: node.v18.brew.sigma.s0fractal.io
```

## 3. Collapse

```
ts.sigma --needs--> runtime
runtime --edge--> brew_node
runtime --edge--> sh_node
runtime --edge--> wasm_node
runtime --edge--> spore
```

Pick the cheapest valid edge; cache it; pass to offspring.

## 4. CLI Lens (plan)

```
λ edges ts
λ edges --missing
λ edges --collapse
λ edges --sync
```
